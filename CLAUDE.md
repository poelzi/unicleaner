@AGENTS.md

## Active Technologies
- Rust 1.85+ (stable, edition 2024) + clap 4 (CLI), serde/toml (config), rayon (parallelism), once_cell (static init), unicode-segmentation (graphemes), globset (rules), owo-colors (output) (004-fix-review-findings)
- TOML configuration files, filesystem scanning (004-fix-review-findings)

## Recent Changes
- 004-fix-review-findings: Added Rust 1.85+ (stable, edition 2024) + clap 4 (CLI), serde/toml (config), rayon (parallelism), once_cell (static init), unicode-segmentation (graphemes), globset (rules), owo-colors (output)
