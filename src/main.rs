use std::process;

fn main() {
    // Placeholder for CLI entry point
    // Will be implemented in Phase 3 after foundation is complete
    eprintln!("Unicleaner - Malicious Unicode Detector");
    eprintln!("Foundation phase in progress...");
    process::exit(0);
}
